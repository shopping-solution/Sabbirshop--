import React, { useState, useEffect } from 'react';

// --- DUMMY DATA ---
const PRODUCTS = [
  { id: 1, name: "Premium Silk Panjabi - Midnight Blue", category: "Panjabi", price: 2250, originalPrice: 2800, rating: 4.8, reviews: 124, image: "https://images.unsplash.com/photo-1593030761757-71fae45fa0e7?auto=format&fit=crop&w=600&q=80", badge: "Best Seller" },
  { id: 2, name: "Slim Fit Denim Jeans - Washed Black", category: "Jeans", price: 1450, originalPrice: 1800, rating: 4.5, reviews: 89, image: "https://images.unsplash.com/photo-1542272604-787c3835535d?auto=format&fit=crop&w=600&q=80", badge: "New Arrival" },
  { id: 3, name: "Classic Oxford Cotton Shirt", category: "Shirts", price: 1250, originalPrice: 1500, rating: 4.6, reviews: 56, image: "https://images.unsplash.com/photo-1596755094514-f87e32f85e2c?auto=format&fit=crop&w=600&q=80" },
  { id: 4, name: "Men's Faux Leather Biker Jacket", category: "Jackets", price: 3500, originalPrice: 4500, rating: 4.9, reviews: 210, image: "https://images.unsplash.com/photo-1551028719-00167b16eac5?auto=format&fit=crop&w=600&q=80", badge: "Flash Sale" },
  { id: 5, name: "Essential Crewneck T-Shirt", category: "T-Shirts", price: 450, originalPrice: 600, rating: 4.3, reviews: 34, image: "https://images.unsplash.com/photo-1521572163474-6864f9cf17ab?auto=format&fit=crop&w=600&q=80" },
  { id: 6, name: "Formal Leather Oxford Shoes", category: "Shoes", price: 2850, originalPrice: 3500, rating: 4.7, reviews: 112, image: "https://images.unsplash.com/photo-1614252235316-8c857d38b5f4?auto=format&fit=crop&w=600&q=80" },
  { id: 7, name: "Premium Chino Pants - Olive", category: "Pants", price: 1150, originalPrice: 1400, rating: 4.4, reviews: 45, image: "https://images.unsplash.com/photo-1624378439575-d8705ad7ae80?auto=format&fit=crop&w=600&q=80" },
  { id: 8, name: "Winter Fleece Hoodie", category: "Jackets", price: 950, originalPrice: 1200, rating: 4.6, reviews: 78, image: "https://images.unsplash.com/photo-1556821840-3a63f95609a7?auto=format&fit=crop&w=600&q=80", badge: "Trending" },
];

const CATEGORIES = ["All", "Panjabi", "Shirts", "T-Shirts", "Jeans", "Pants", "Jackets", "Shoes", "Accessories"];

// --- ICONS (Inline SVGs for zero dependencies) ---
const IconCart = () => <svg className="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M3 3h2l.4 2M7 13h10l4-8H5.4M7 13L5.4 5M7 13l-2.293 2.293c-.63.63-.184 1.707.707 1.707H17m0 0a2 2 0 100 4 2 2 0 000-4zm-8 2a2 2 0 11-4 0 2 2 0 014 0z" /></svg>;
const IconUser = () => <svg className="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" /></svg>;
const IconSearch = () => <svg className="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" /></svg>;
const IconStar = ({ filled }) => <svg className={`w-4 h-4 ${filled ? 'text-yellow-400' : 'text-gray-300'}`} fill="currentColor" viewBox="0 0 20 20" xmlns="http://www.w3.org/2000/svg"><path d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z" /></svg>;

export default function ShoppingSolutionApp() {
  const [currentView, setCurrentView] = useState('home'); // home, shop, product, live, cart, checkout
  const [selectedProduct, setSelectedProduct] = useState(null);
  const [cart, setCart] = useState([]);
  
  // Navigation Helper
  const navigate = (view, product = null) => {
    setCurrentView(view);
    if (product) setSelectedProduct(product);
    window.scrollTo(0, 0);
  };

  // Cart Handlers
  const addToCart = (product, quantity = 1, size = 'M', color = 'Default') => {
    setCart(prev => {
      const existing = prev.find(item => item.id === product.id && item.size === size);
      if (existing) {
        return prev.map(item => item.id === product.id && item.size === size ? { ...item, quantity: item.quantity + quantity } : item);
      }
      return [...prev, { ...product, quantity, size, color }];
    });
    alert("Item added to cart!");
  };

  const updateQuantity = (id, size, delta) => {
    setCart(prev => prev.map(item => {
      if (item.id === id && item.size === size) {
        return { ...item, quantity: Math.max(1, item.quantity + delta) };
      }
      return item;
    }));
  };

  const removeFromCart = (id, size) => {
    setCart(prev => prev.filter(item => !(item.id === id && item.size === size)));
  };

  const cartTotal = cart.reduce((sum, item) => sum + (item.price * item.quantity), 0);

  return (
    <div className="min-h-screen bg-gray-50 font-sans text-gray-900 selection:bg-red-600 selection:text-white">
      {/* HEADER */}
      <header className="sticky top-0 z-50 bg-slate-900 text-white shadow-lg">
        <div className="container mx-auto px-4 py-3 flex items-center justify-between">
          <div className="flex items-center gap-4">
            <h1 className="text-xl md:text-2xl font-black tracking-wider cursor-pointer text-white" onClick={() => navigate('home')}>
              SHOPPING <span className="text-red-500">SOLUTION</span>
            </h1>
          </div>
          
          <div className="hidden md:flex flex-1 max-w-xl mx-8">
            <div className="relative w-full">
              <input type="text" placeholder="Search for Panjabi, Shirts, Jeans..." className="w-full pl-4 pr-10 py-2 rounded-full text-black focus:outline-none focus:ring-2 focus:ring-red-500" />
              <button className="absolute right-2 top-1.5 text-gray-500 hover:text-red-500"><IconSearch /></button>
            </div>
          </div>

          <div className="flex items-center gap-4 md:gap-6">
            <button className="hover:text-red-400 transition flex flex-col items-center">
              <IconUser />
              <span className="text-xs mt-1 hidden md:block">Account</span>
            </button>
            <button className="hover:text-red-400 transition flex flex-col items-center relative" onClick={() => navigate('cart')}>
              <IconCart />
              {cart.length > 0 && <span className="absolute -top-2 -right-2 bg-red-600 text-white text-[10px] font-bold px-1.5 py-0.5 rounded-full">{cart.length}</span>}
              <span className="text-xs mt-1 hidden md:block">Cart (৳{cartTotal})</span>
            </button>
          </div>
        </div>
        
        {/* Navigation Bar */}
        <nav className="bg-slate-800 border-t border-slate-700 overflow-x-auto">
          <ul className="container mx-auto px-4 py-3 flex gap-6 text-sm font-semibold min-w-max">
            <li className="cursor-pointer hover:text-red-400" onClick={() => navigate('home')}>HOME</li>
            <li className="cursor-pointer hover:text-red-400" onClick={() => navigate('shop')}>SHOP ALL</li>
            <li className="cursor-pointer text-red-400 flex items-center gap-1" onClick={() => navigate('live')}>
              <span className="w-2 h-2 rounded-full bg-red-500 animate-pulse"></span> LIVE SHOPPING
            </li>
            <li className="cursor-pointer hover:text-red-400" onClick={() => navigate('shop')}>NEW ARRIVALS</li>
            <li className="cursor-pointer hover:text-red-400 text-yellow-400">FLASH SALE ⚡</li>
          </ul>
        </nav>
      </header>

      {/* MAIN CONTENT AREA */}
      <main className="min-h-[70vh]">
        {currentView === 'home' && <HomeView navigate={navigate} />}
        {currentView === 'shop' && <ShopView navigate={navigate} />}
        {currentView === 'product' && <ProductDetailView product={selectedProduct} addToCart={addToCart} navigate={navigate} />}
        {currentView === 'live' && <LiveShoppingView navigate={navigate} />}
        {currentView === 'cart' && <CartView cart={cart} updateQuantity={updateQuantity} removeFromCart={removeFromCart} cartTotal={cartTotal} navigate={navigate} />}
        {currentView === 'checkout' && <CheckoutView cartTotal={cartTotal} navigate={navigate} />}
      </main>

      {/* FOOTER */}
      <footer className="bg-slate-900 text-gray-300 py-12 mt-12 border-t-4 border-red-600">
        <div className="container mx-auto px-4 grid grid-cols-1 md:grid-cols-4 gap-8">
          <div>
            <h2 className="text-xl font-black text-white mb-4">SHOPPING <span className="text-red-500">SOLUTION</span></h2>
            <p className="text-sm leading-relaxed mb-4">Premium men's fashion destination in Bangladesh. Quality products, fast delivery, and trustworthy service.</p>
            <p className="text-sm font-bold">Help Line: 096XX-XXXXXX</p>
          </div>
          <div>
            <h3 className="text-white font-bold mb-4 uppercase">Customer Care</h3>
            <ul className="space-y-2 text-sm">
              <li><a href="#" className="hover:text-red-400">Help Center</a></li>
              <li><a href="#" className="hover:text-red-400">How to Buy</a></li>
              <li><a href="#" className="hover:text-red-400">Returns & Refunds</a></li>
              <li><a href="#" className="hover:text-red-400">Contact Us</a></li>
            </ul>
          </div>
          <div>
            <h3 className="text-white font-bold mb-4 uppercase">Trust & Policies</h3>
            <ul className="space-y-3 text-sm">
              <li className="flex items-center gap-2"><span className="text-xl">🚚</span> সারা বাংলাদেশে ডেলিভারি</li>
              <li className="flex items-center gap-2"><span className="text-xl">↩️</span> ৭ দিনের রিটার্ন পলিসি</li>
              <li className="flex items-center gap-2"><span className="text-xl">🛡️</span> ১০০% অরিজিনাল প্রোডাক্ট</li>
            </ul>
          </div>
          <div>
            <h3 className="text-white font-bold mb-4 uppercase">Payment Methods</h3>
            <div className="flex flex-wrap gap-2">
              <span className="bg-pink-600 text-white px-2 py-1 rounded text-xs font-bold">bKash</span>
              <span className="bg-orange-500 text-white px-2 py-1 rounded text-xs font-bold">Nagad</span>
              <span className="bg-green-600 text-white px-2 py-1 rounded text-xs font-bold">Cash on Delivery</span>
              <span className="bg-blue-600 text-white px-2 py-1 rounded text-xs font-bold">Visa/Mastercard</span>
            </div>
          </div>
        </div>
        <div className="container mx-auto px-4 mt-8 pt-8 border-t border-slate-700 text-center text-xs text-gray-500">
          © {new Date().getFullYear()} Shopping Solution BD. All rights reserved. Designed for Men's Fashion.
        </div>
      </footer>
    </div>
  );
}

// ==========================================
// VIEWS COMPONENTS
// ==========================================

function HomeView({ navigate }) {
  const [timeLeft, setTimeLeft] = useState({ h: 12, m: 45, s: 30 });

  // Dummy countdown timer
  useEffect(() => {
    const timer = setInterval(() => {
      setTimeLeft(prev => {
        let { h, m, s } = prev;
        if (s > 0) s--;
        else { s = 59; if (m > 0) m--; else { m = 59; h--; } }
        return { h, m, s };
      });
    }, 1000);
    return () => clearInterval(timer);
  }, []);

  return (
    <div>
      {/* HERO BANNER */}
      <div className="relative bg-slate-900 h-[60vh] flex items-center justify-center overflow-hidden">
        <img src="https://images.unsplash.com/photo-1617137984095-74e4e5e3613f?auto=format&fit=crop&w=1600&q=80" alt="Men Fashion" className="absolute inset-0 w-full h-full object-cover opacity-30" />
        <div className="relative z-10 text-center text-white px-4">
          <span className="bg-red-600 text-white px-3 py-1 rounded-full text-xs font-bold uppercase tracking-wider mb-4 inline-block">Eid Collection 2026</span>
          <h1 className="text-4xl md:text-6xl font-black mb-4 uppercase">Upgrade Your Wardrobe</h1>
          <p className="text-lg md:text-xl mb-8 text-gray-300">Premium Panjabis, Shirts & Denims for the Modern Bangladeshi Man.</p>
          <button onClick={() => navigate('shop')} className="bg-red-600 hover:bg-red-700 text-white font-bold py-3 px-8 rounded-full shadow-lg hover:shadow-red-500/50 transition transform hover:-translate-y-1">
            অর্ডার করুন | Shop Now
          </button>
        </div>
      </div>

      {/* CATEGORIES */}
      <div className="container mx-auto px-4 py-12">
        <h2 className="text-2xl font-bold mb-6 text-slate-900">Explore Categories</h2>
        <div className="flex overflow-x-auto gap-6 pb-4 scrollbar-hide">
          {["Panjabi", "Shirts", "T-Shirts", "Jeans", "Jackets", "Shoes"].map((cat, i) => (
            <div key={i} className="flex flex-col items-center gap-2 cursor-pointer min-w-[100px] group" onClick={() => navigate('shop')}>
              <div className="w-20 h-20 rounded-full bg-slate-200 overflow-hidden border-2 border-transparent group-hover:border-red-500 transition">
                <img src={`https://images.unsplash.com/photo-${1500000000000 + i * 10000}?auto=format&fit=crop&w=200&q=80`} alt={cat} className="w-full h-full object-cover mix-blend-multiply" onError={(e)=>e.target.src="https://images.unsplash.com/photo-1542272604-787c3835535d?auto=format&fit=crop&w=200&q=80"} />
              </div>
              <span className="text-sm font-semibold group-hover:text-red-600">{cat}</span>
            </div>
          ))}
        </div>
      </div>

      {/* FLASH SALE */}
      <div className="bg-red-50 py-12">
        <div className="container mx-auto px-4">
          <div className="flex flex-col md:flex-row justify-between items-center mb-8">
            <div className="flex items-center gap-4">
              <h2 className="text-2xl font-bold text-slate-900">Flash Sale ⚡</h2>
              <div className="flex gap-2">
                <span className="bg-slate-900 text-white px-2 py-1 rounded font-mono text-sm">{String(timeLeft.h).padStart(2,'0')}</span>:
                <span className="bg-slate-900 text-white px-2 py-1 rounded font-mono text-sm">{String(timeLeft.m).padStart(2,'0')}</span>:
                <span className="bg-slate-900 text-white px-2 py-1 rounded font-mono text-sm">{String(timeLeft.s).padStart(2,'0')}</span>
              </div>
            </div>
            <button onClick={() => navigate('shop')} className="text-red-600 font-semibold hover:underline mt-4 md:mt-0">See All &gt;</button>
          </div>
          
          <div className="grid grid-cols-2 md:grid-cols-4 gap-4 md:gap-6">
            {PRODUCTS.slice(0, 4).map(product => (
              <ProductCard key={product.id} product={product} navigate={navigate} />
            ))}
          </div>
        </div>
      </div>
    </div>
  );
}

function ShopView({ navigate }) {
  return (
    <div className="container mx-auto px-4 py-8 flex flex-col md:flex-row gap-8">
      {/* Filters Sidebar */}
      <aside className="w-full md:w-64 flex-shrink-0">
        <h3 className="font-bold text-lg mb-4 uppercase border-b pb-2">Filters</h3>
        <div className="mb-6">
          <h4 className="font-semibold mb-3">Category</h4>
          <ul className="space-y-2 text-sm text-gray-600">
            {CATEGORIES.map(c => (
              <li key={c} className="flex items-center gap-2 cursor-pointer hover:text-red-600">
                <input type="checkbox" className="accent-red-600" /> {c}
              </li>
            ))}
          </ul>
        </div>
        <div className="mb-6">
          <h4 className="font-semibold mb-3">Price Range</h4>
          <input type="range" className="w-full accent-red-600" min="100" max="5000" />
          <div className="flex justify-between text-xs mt-2">
            <span>৳100</span>
            <span>৳5,000</span>
          </div>
        </div>
      </aside>

      {/* Product Grid */}
      <div className="flex-1">
        <div className="flex justify-between items-center mb-6">
          <h2 className="text-xl font-bold">Men's Clothing ({PRODUCTS.length} Items)</h2>
          <select className="border-gray-300 border rounded py-1 px-3 text-sm focus:ring-red-500 focus:border-red-500">
            <option>Popularity</option>
            <option>Price: Low to High</option>
            <option>Price: High to Low</option>
            <option>Newest Arrivals</option>
          </select>
        </div>
        <div className="grid grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-4 md:gap-6">
          {PRODUCTS.map(product => (
            <ProductCard key={product.id} product={product} navigate={navigate} />
          ))}
        </div>
      </div>
    </div>
  );
}

function ProductDetailView({ product, addToCart, navigate }) {
  if (!product) return null;
  const discount = Math.round(((product.originalPrice - product.price) / product.originalPrice) * 100);
  const [qty, setQty] = useState(1);
  const [selectedSize, setSelectedSize] = useState('M');

  return (
    <div className="container mx-auto px-4 py-8">
      <button onClick={() => navigate('shop')} className="text-sm text-gray-500 mb-6 hover:text-red-600">&larr; Back to Shop</button>
      
      <div className="flex flex-col md:flex-row gap-10">
        {/* Images */}
        <div className="w-full md:w-1/2 flex gap-4">
          <div className="flex flex-col gap-2 w-20 hidden md:flex">
            {[1,2,3].map(i => (
              <img key={i} src={product.image} className="w-full h-24 object-cover cursor-pointer border hover:border-red-500" alt="thumb" />
            ))}
          </div>
          <div className="flex-1">
            <img src={product.image} alt={product.name} className="w-full h-auto object-cover rounded-lg shadow-sm" />
          </div>
        </div>

        {/* Details */}
        <div className="w-full md:w-1/2">
          <span className="text-red-600 text-sm font-bold uppercase tracking-wider">{product.category}</span>
          <h1 className="text-2xl md:text-4xl font-bold mt-2 mb-4 text-slate-900">{product.name}</h1>
          
          <div className="flex items-center gap-2 mb-6">
            <div className="flex"><IconStar filled/><IconStar filled/><IconStar filled/><IconStar filled/><IconStar filled/></div>
            <span className="text-sm text-gray-500">({product.reviews} Reviews)</span>
          </div>

          <div className="flex items-end gap-4 mb-6">
            <span className="text-4xl font-black text-red-600">৳{product.price}</span>
            <span className="text-lg text-gray-400 line-through mb-1">৳{product.originalPrice}</span>
            <span className="bg-yellow-400 text-slate-900 text-xs font-bold px-2 py-1 rounded mb-2">-{discount}% OFF</span>
          </div>

          {/* Selectors */}
          <div className="mb-6">
            <h4 className="font-semibold mb-2">Size</h4>
            <div className="flex gap-3">
              {['S', 'M', 'L', 'XL'].map(size => (
                <button key={size} onClick={() => setSelectedSize(size)} 
                  className={`w-12 h-12 border flex items-center justify-center rounded transition ${selectedSize === size ? 'bg-slate-900 text-white border-slate-900' : 'bg-white hover:border-slate-900'}`}>
                  {size}
                </button>
              ))}
            </div>
          </div>

          <div className="flex gap-4 mb-8">
            <div className="flex border rounded w-32">
              <button className="px-4 py-2 hover:bg-gray-100 font-bold" onClick={() => setQty(q => Math.max(1, q-1))}>-</button>
              <input type="text" value={qty} readOnly className="w-full text-center focus:outline-none" />
              <button className="px-4 py-2 hover:bg-gray-100 font-bold" onClick={() => setQty(q => q+1)}>+</button>
            </div>
          </div>

          <div className="flex flex-col sm:flex-row gap-4 mb-8">
            <button onClick={() => addToCart(product, qty, selectedSize)} className="flex-1 bg-white border-2 border-slate-900 text-slate-900 font-bold py-3 rounded hover:bg-slate-50 transition">
              Add to Cart
            </button>
            <button onClick={() => { addToCart(product, qty, selectedSize); navigate('checkout'); }} className="flex-1 bg-red-600 text-white font-bold py-3 rounded shadow-lg shadow-red-500/30 hover:bg-red-700 transition">
              অর্ডার করুন (Buy Now)
            </button>
          </div>

          {/* Mini Badges */}
          <div className="bg-gray-100 p-4 rounded-lg flex flex-col gap-2 text-sm text-gray-700">
             <p>✔️ Cash on Delivery available across Bangladesh</p>
             <p>✔️ 7 Days Easy Return Policy</p>
             <p>✔️ 100% Authentic Product</p>
          </div>
        </div>
      </div>
    </div>
  );
}

function LiveShoppingView({ navigate }) {
  const lives = [
    { id: 1, title: "Men's Eid Collection Panjabi Showcase", host: "Afnan Fashion", viewers: 1240, img: "https://images.unsplash.com/photo-1616150730030-f86055bc5eb8?auto=format&fit=crop&w=400&q=80", tag: PRODUCTS[0] },
    { id: 2, title: "Clearance Sale: Winter Jackets & Hoodies", host: "Shopping Solution Official", viewers: 856, img: "https://images.unsplash.com/photo-1551028719-00167b16eac5?auto=format&fit=crop&w=400&q=80", tag: PRODUCTS[3] },
    { id: 3, title: "Premium Denim Try-on Haul", host: "Style BD", viewers: 530, img: "https://images.unsplash.com/photo-1542272604-787c3835535d?auto=format&fit=crop&w=400&q=80", tag: PRODUCTS[1] }
  ];

  return (
    <div className="container mx-auto px-4 py-8">
      <div className="flex items-center gap-3 mb-8">
        <div className="w-4 h-4 bg-red-600 rounded-full animate-pulse"></div>
        <h2 className="text-2xl font-bold uppercase text-slate-900">Live Shopping</h2>
      </div>

      <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
        {lives.map(live => (
          <div key={live.id} className="bg-white rounded-xl shadow-md overflow-hidden relative group">
            {/* Video Placeholder */}
            <div className="relative h-[400px] bg-black overflow-hidden flex items-center justify-center cursor-pointer">
              <img src={live.img} alt="Live" className="absolute inset-0 w-full h-full object-cover opacity-60 group-hover:scale-105 transition duration-700" />
              
              {/* Overlays */}
              <div className="absolute top-4 left-4 bg-red-600 text-white text-xs font-bold px-2 py-1 rounded shadow animate-pulse">LIVE NOW</div>
              <div className="absolute top-4 right-4 bg-black/50 text-white text-xs font-bold px-2 py-1 rounded flex items-center gap-1">👁️ {live.viewers}</div>
              
              {/* Play Button */}
              <div className="z-10 bg-white/20 p-4 rounded-full backdrop-blur-sm border border-white/50">
                 <svg className="w-8 h-8 text-white ml-1" fill="currentColor" viewBox="0 0 20 20"><path d="M4 4l12 6-12 6z" /></svg>
              </div>

              {/* Title Overlay */}
              <div className="absolute bottom-0 left-0 w-full p-4 bg-gradient-to-t from-black/90 to-transparent">
                <h3 className="text-white font-bold">{live.title}</h3>
                <p className="text-gray-300 text-sm">{live.host}</p>
              </div>
            </div>

            {/* Tagged Product Area */}
            <div className="p-4 border-t">
              <p className="text-xs text-gray-500 font-bold mb-2 uppercase">Currently Showcasing</p>
              <div className="flex items-center gap-3 bg-gray-50 p-2 rounded cursor-pointer hover:bg-gray-100 transition" onClick={() => navigate('product', live.tag)}>
                <img src={live.tag.image} className="w-12 h-12 rounded object-cover" alt="product" />
                <div className="flex-1">
                  <h4 className="text-sm font-semibold truncate">{live.tag.name}</h4>
                  <p className="text-red-600 font-bold text-sm">৳{live.tag.price}</p>
                </div>
                <button className="bg-slate-900 text-white text-xs px-3 py-1.5 rounded">Buy</button>
              </div>
            </div>
          </div>
        ))}
      </div>
    </div>
  );
}

function CartView({ cart, updateQuantity, removeFromCart, cartTotal, navigate }) {
  if (cart.length === 0) {
    return (
      <div className="container mx-auto px-4 py-16 text-center">
        <div className="mb-4 flex justify-center text-gray-300"><IconCart size={64} /></div>
        <h2 className="text-2xl font-bold mb-4">Your cart is empty</h2>
        <button onClick={() => navigate('shop')} className="bg-slate-900 text-white px-6 py-2 rounded font-bold hover:bg-red-600 transition">Continue Shopping</button>
      </div>
    );
  }

  const deliveryCharge = 60; // Inside Dhaka dummy

  return (
    <div className="container mx-auto px-4 py-8">
      <h2 className="text-2xl font-bold mb-8">Shopping Cart</h2>
      
      <div className="flex flex-col lg:flex-row gap-8">
        <div className="flex-1 space-y-4">
          {cart.map((item) => (
            <div key={`${item.id}-${item.size}`} className="flex gap-4 bg-white p-4 rounded shadow-sm border border-gray-100">
              <img src={item.image} alt={item.name} className="w-24 h-24 object-cover rounded" />
              <div className="flex-1 flex flex-col justify-between">
                <div>
                  <h3 className="font-semibold text-lg leading-tight text-slate-900">{item.name}</h3>
                  <p className="text-sm text-gray-500">Size: {item.size}</p>
                  <p className="text-red-600 font-bold mt-1">৳{item.price}</p>
                </div>
                <div className="flex justify-between items-center mt-4">
                  <div className="flex border rounded w-24 h-8 items-center">
                    <button className="flex-1 hover:bg-gray-100" onClick={() => updateQuantity(item.id, item.size, -1)}>-</button>
                    <span className="flex-1 text-center text-sm">{item.quantity}</span>
                    <button className="flex-1 hover:bg-gray-100" onClick={() => updateQuantity(item.id, item.size, 1)}>+</button>
                  </div>
                  <button className="text-sm text-gray-400 hover:text-red-600 underline" onClick={() => removeFromCart(item.id, item.size)}>Remove</button>
                </div>
              </div>
            </div>
          ))}
        </div>

        {/* Summary */}
        <div className="w-full lg:w-80 bg-white p-6 rounded shadow-sm border border-gray-100 h-fit sticky top-24">
          <h3 className="font-bold text-lg mb-4 border-b pb-2">Order Summary</h3>
          <div className="space-y-2 mb-4 text-sm">
            <div className="flex justify-between"><span>Subtotal</span><span>৳{cartTotal}</span></div>
            <div className="flex justify-between"><span>Delivery Fee</span><span>৳{deliveryCharge}</span></div>
          </div>
          
          <div className="border-t pt-4 mb-6">
            <div className="flex justify-between font-bold text-lg"><span>Total</span><span className="text-red-600">৳{cartTotal + deliveryCharge}</span></div>
            <p className="text-xs text-right text-gray-500 mt-1">VAT included, where applicable</p>
          </div>

          <div className="mb-6 flex gap-2">
            <input type="text" placeholder="Promo Code" className="border rounded px-3 py-2 w-full text-sm" />
            <button className="bg-slate-900 text-white px-4 py-2 rounded text-sm font-bold">Apply</button>
          </div>

          <button onClick={() => navigate('checkout')} className="w-full bg-red-600 text-white font-bold py-3 rounded shadow-lg hover:bg-red-700 transition">
            PROCEED TO CHECKOUT
          </button>
        </div>
      </div>
    </div>
  );
}

function CheckoutView({ cartTotal, navigate }) {
  const [paymentMethod, setPaymentMethod] = useState('cod');

  return (
    <div className="container mx-auto px-4 py-8">
      <h2 className="text-2xl font-bold mb-8">Checkout</h2>
      
      <div className="flex flex-col lg:flex-row gap-8">
        {/* Forms */}
        <div className="flex-1 space-y-8">
          <section className="bg-white p-6 rounded shadow-sm border border-gray-100">
            <h3 className="font-bold text-lg mb-4">1. Shipping Address</h3>
            <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
              <input type="text" placeholder="Full Name" className="border rounded px-3 py-2 w-full focus:ring-red-500 focus:border-red-500" />
              <input type="text" placeholder="Phone Number (e.g. 017XXXXXXX)" className="border rounded px-3 py-2 w-full focus:ring-red-500 focus:border-red-500" />
              <input type="text" placeholder="City" className="border rounded px-3 py-2 w-full focus:ring-red-500 focus:border-red-500" />
              <input type="text" placeholder="Area / Thana" className="border rounded px-3 py-2 w-full focus:ring-red-500 focus:border-red-500" />
              <textarea placeholder="Full Address (House, Road, Block)" className="border rounded px-3 py-2 w-full md:col-span-2 focus:ring-red-500 focus:border-red-500"></textarea>
            </div>
          </section>

          <section className="bg-white p-6 rounded shadow-sm border border-gray-100">
            <h3 className="font-bold text-lg mb-4">2. Select Payment Method</h3>
            <div className="grid grid-cols-1 sm:grid-cols-2 gap-4">
              {/* COD */}
              <label className={`border-2 rounded p-4 flex items-center gap-3 cursor-pointer transition ${paymentMethod === 'cod' ? 'border-red-600 bg-red-50' : 'border-gray-200'}`}>
                <input type="radio" name="payment" value="cod" checked={paymentMethod === 'cod'} onChange={(e) => setPaymentMethod(e.target.value)} className="accent-red-600 w-5 h-5" />
                <div>
                  <span className="font-bold block">Cash on Delivery</span>
                  <span className="text-xs text-gray-500">Pay when you receive the product</span>
                </div>
              </label>

              {/* bKash */}
              <label className={`border-2 rounded p-4 flex items-center gap-3 cursor-pointer transition ${paymentMethod === 'bkash' ? 'border-pink-600 bg-pink-50' : 'border-gray-200'}`}>
                <input type="radio" name="payment" value="bkash" checked={paymentMethod === 'bkash'} onChange={(e) => setPaymentMethod(e.target.value)} className="accent-pink-600 w-5 h-5" />
                <div>
                  <span className="font-bold text-pink-600 block">bKash Payment</span>
                  <span className="text-xs text-gray-500">Instant secure mobile payment</span>
                </div>
              </label>

              {/* Nagad */}
              <label className={`border-2 rounded p-4 flex items-center gap-3 cursor-pointer transition ${paymentMethod === 'nagad' ? 'border-orange-500 bg-orange-50' : 'border-gray-200'}`}>
                <input type="radio" name="payment" value="nagad" checked={paymentMethod === 'nagad'} onChange={(e) => setPaymentMethod(e.target.value)} className="accent-orange-500 w-5 h-5" />
                <div>
                  <span className="font-bold text-orange-600 block">Nagad Payment</span>
                  <span className="text-xs text-gray-500">Instant secure mobile payment</span>
                </div>
              </label>

              {/* Cards */}
              <label className={`border-2 rounded p-4 flex items-center gap-3 cursor-pointer transition ${paymentMethod === 'card' ? 'border-blue-600 bg-blue-50' : 'border-gray-200'}`}>
                <input type="radio" name="payment" value="card" checked={paymentMethod === 'card'} onChange={(e) => setPaymentMethod(e.target.value)} className="accent-blue-600 w-5 h-5" />
                <div>
                  <span className="font-bold block">Credit / Debit Card</span>
                  <span className="text-xs text-gray-500">Visa, Mastercard, Amex</span>
                </div>
              </label>
            </div>
          </section>
        </div>

        {/* Confirm Box */}
        <div className="w-full lg:w-80 bg-white p-6 rounded shadow-sm border border-gray-100 h-fit sticky top-24">
          <h3 className="font-bold text-lg mb-4 border-b pb-2">Final Summary</h3>
          <div className="flex justify-between font-bold text-xl mb-6"><span>Total to Pay</span><span className="text-red-600">৳{cartTotal + 60}</span></div>
          
          <button onClick={() => { alert('Order Placed Successfully! Mock demo complete.'); navigate('home'); }} className="w-full bg-slate-900 text-white font-bold py-4 rounded shadow-lg hover:bg-black transition text-lg">
            CONFIRM ORDER
          </button>
          <p className="text-xs text-center text-gray-500 mt-4">By clicking "Confirm Order", you agree to our Terms and Conditions.</p>
        </div>
      </div>
    </div>
  );
}

// ==========================================
// UTILITY COMPONENTS
// ==========================================

function ProductCard({ product, navigate }) {
  const discount = Math.round(((product.originalPrice - product.price) / product.originalPrice) * 100);
  
  return (
    <div className="bg-white rounded-lg overflow-hidden shadow hover:shadow-xl transition-shadow duration-300 group cursor-pointer border border-gray-100 relative flex flex-col" onClick={() => navigate('product', product)}>
      {/* Badges */}
      {discount > 0 && <span className="absolute top-2 left-2 bg-red-600 text-white text-[10px] font-bold px-2 py-1 rounded z-10">-{discount}%</span>}
      {product.badge && <span className="absolute top-2 right-2 bg-yellow-400 text-slate-900 text-[10px] font-bold px-2 py-1 rounded z-10">{product.badge}</span>}
      
      {/* Image */}
      <div className="relative h-48 md:h-64 overflow-hidden">
        <img src={product.image} alt={product.name} className="w-full h-full object-cover transform group-hover:scale-105 transition duration-500" />
        <div className="absolute inset-0 bg-black bg-opacity-0 group-hover:bg-opacity-10 transition duration-300 flex items-center justify-center">
          <button className="opacity-0 group-hover:opacity-100 bg-white text-slate-900 font-bold py-2 px-4 rounded shadow-lg transform translate-y-4 group-hover:translate-y-0 transition duration-300">
            Quick View
          </button>
        </div>
      </div>
      
      {/* Details */}
      <div className="p-4 flex flex-col flex-grow">
        <span className="text-[10px] text-gray-400 font-bold uppercase tracking-wide mb-1">{product.category}</span>
        <h3 className="font-semibold text-gray-800 text-sm md:text-base leading-tight mb-2 line-clamp-2">{product.name}</h3>
        
        <div className="mt-auto">
          <div className="flex items-center gap-1 mb-2">
            <IconStar filled/><IconStar filled/><IconStar filled/><IconStar filled/><IconStar filled={false}/>
            <span className="text-[10px] text-gray-500 ml-1">({product.reviews})</span>
          </div>
          <div className="flex items-end gap-2">
            <span className="text-red-600 font-bold text-lg">৳{product.price}</span>
            {product.originalPrice > product.price && <span className="text-gray-400 text-xs line-through mb-1">৳{product.originalPrice}</span>}
          </div>
        </div>
      </div>
    </div>
  );
}